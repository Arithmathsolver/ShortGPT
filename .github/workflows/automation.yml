import os
import sys
import time
import traceback
from pathlib import Path

# Ensure local repository path layouts take precedence in memory
_here = Path(__file__).parent.resolve()
if str(_here) not in sys.path:
    sys.path.insert(0, str(_here))

# =====================================================================
# 🛡️ SMART GEMINI-TO-GROQ FAILOVER & RATE LIMIT MONKEY PATCH
# This protects your pipeline. If Gemini hits a per-minute or a 
# hard daily limit, it shifts requests instantly to Groq's open-source 
# OpenAI-compatible API endpoint layout.
# =====================================================================
try:
    from shortGPT.gpt import gpt_utils
    from shortGPT.gpt.gpt_utils import gpt3_5_turbo_completion
    
    # Check if we have already patched to prevent endless recursion loops
    if hasattr(gpt_utils, 'generate_text_gemini') and not hasattr(gpt_utils, '_fallback_active'):
        gpt_utils._fallback_active = True
        gpt_utils._original_generate_text_gemini = gpt_utils.generate_text_gemini
        
        def smart_failover_gemini_wrapper(prompt, system_prompt=None):
            max_retries = 3
            base_delay = 5  # Quick buffer for minor network hiccups
            
            for attempt in range(max_retries):
                try:
                    # 1. Try running Gemini normally
                    return gpt_utils._original_generate_text_gemini(prompt, system_prompt)
                except Exception as e:
                    err_msg = str(e)
                    
                    # 2. Check if it's a daily limit wall or total exhaust block
                    if "Quota exceeded" in err_msg or "GenerateRequestsPerDay" in err_msg or "ResourceExhausted" in err_msg:
                        print("\n🛑 [GEMINI HARD LIMIT HIT]: Daily 20-request free cap exceeded.")
                        print("🔄 FALLBACK INITIATED: Routing text request through Groq API layout instead...")
                        
                        # Pack prompts for ShortGPT's built-in OpenAI function layer
                        combined_prompt = f"{system_prompt}\n\n{prompt}" if system_prompt else prompt
                        return gpt3_5_turbo_completion(combined_prompt)
                        
                    # 3. Handle a standard 429 per-minute request limit burst
                    elif "429" in err_msg:
                        print(f"\n⚠️ [Gemini Minute Spike]: Sleeping {base_delay}s... (Attempt {attempt + 1}/{max_retries})")
                        time.sleep(base_delay)
                        continue
                        
                    # If it's an unrelated logic error, pass it upward
                    raise e
                    
            # Ultimate failover if the retry loops burn out completely
            print("\n🔄 [EMERGENCY OVERRIDE]: Exhausted Gemini retries. Pushing to Groq...")
            combined_prompt = f"{system_prompt}\n\n{prompt}" if system_prompt else prompt
            return gpt3_5_turbo_completion(combined_prompt)
            
        # Swap the functions in live system memory globally
        gpt_utils.generate_text_gemini = smart_failover_gemini_wrapper
        print("✅ Core Optimization: Gemini-to-Groq daily failover architecture engaged.")
except Exception as patch_err:
    print(f"⚠️ Failover routing patch notice: {patch_err}")
# =====================================================================

from shortGPT.config.api_db import ApiKeyManager
from shortGPT.config.asset_db import AssetDatabase
from shortGPT.audio.edge_voice_module import EdgeTTSVoiceModule
from shortGPT.engine.facts_short_engine import FactsShortEngine

def get_automation_settings():
    return {
        "language": os.getenv("SHORTGPT_LANG", "English"),
        "voice": os.getenv("SHORTGPT_VOICE", "en-US-ChristopherNeural"), 
        "niche_topic": os.getenv("SHORTGPT_NICHE", "Deep Sea Mysteries"),
        "channels_amount": int(os.getenv("SHORTGPT_BATCH_COUNT", "1")),
        "use_images": os.getenv("SHORTGPT_USE_IMAGES", "False").lower() == "true"
    }

def main():
    print("=========================================================")
    print("🚀 STARTING AUTOMATED SHORTGPT MEDIA PIPELINE GENERATION ")
    print("=========================================================")

    try:
        AssetDatabase.initialize_db()
        AssetDatabase.sync_local_assets() 
        print("✅ Asset Database abstraction layers synchronized successfully.")
    except Exception as db_err:
        print(f"⚠️ Database initialization notice: {db_err} (Proceeding...)")

    settings = get_automation_settings()
    topic = settings["niche_topic"]
    language = settings["language"]
    voice_choice = settings["voice"]

    print(f"\n📋 Pipeline Execution Settings:")
    print(f"  • Selected Niche Category : {topic}")
    print(f"  • Target Native Language  : {language}")
    print(f"  • TTS Voice Profile Config: {voice_choice}")
    print(f"  • Batch Run Projections   : {settings['channels_amount']} video(s)\n")

    gemini_key = ApiKeyManager.get_api_key("GEMINI_API_KEY")
    if not gemini_key:
        print("❌ CRITICAL ERROR: GEMINI_API_KEY is not configured in environment or Secrets.")
        sys.exit(1)

    print("DEBUG: Instantiating high-performance EdgeTTSVoiceModule.")
    voice_module = EdgeTTSVoiceModule(voiceName=voice_choice)

    print(f"--- Starting Generation for: {topic} ---")

    try:
        content_engine = FactsShortEngine(
            voiceModule=voice_module,
            facts_type=topic,
            background_video_name="ocean", 
            language=language
        )

        print("🔄 Executing ShortGPT core processing engine loop blocks...")
        
        for step_num, step_logs in content_engine.makeContent():
            print(f"   [Step {step_num}/12 Progress Update]: {step_logs}")

        print("\n=========================================================")
        print("🎉 SUCCESS: Short Video Project compiled and rendered!")
        print("=========================================================")

    except Exception as engine_failure:
        print("\n❌ CRITICAL CRASH: Automation pipeline processing halted.")
        print(f"Error Details: {str(engine_failure)}")
        print("\n--- Python Traceback Log Stack ---")
        traceback.print_exc()
        print("----------------------------------")
        sys.exit(1)

if __name__ == "__main__":
    main()
